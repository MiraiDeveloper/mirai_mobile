import 'package:flutter/material.dart';

class ARfilter extends StatelessWidget {
  const ARfilter({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Ini AR Filter Page"),
    );
  }
}